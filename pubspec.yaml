name: flutter_archena
description: "A new Flutter project for Archena Sports Hub."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.5.4
  provider: ^6.1.2
  intl: ^0.19.0
  qr_flutter: ^4.1.0
  shared_preferences: ^2.2.3
  flutter_svg: ^2.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
